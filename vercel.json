{
    "version": 2,
    "builds": [
        {
            "src": "server.ts",
            "use": "@vercel/node"
        }
    ],
    "buildCommand": "npm run vercel-build",
    "routes": [
        {
            "src": "(.*)",
            "dest": "server.ts"
        }
    ]
}