{
    "version": 2,
    "builds": [
        {
            "src": "dist/server.js",
            "use": "@vercel/node"
        }
    ],
    "buildCommand": "npm run vercel-build",
    "routes": [
        {
            "src": "(.*)",
            "dest": "dist/server.js"
        }
    ]
}